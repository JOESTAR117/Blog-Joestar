<h1>Articles</h1>

<%= link_to 'New Article', new_article_path %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>

<%= render 'longer_featured' %>
<div class="row mb-2">
  <%= render 'medium_featured' %>
  <%= render 'medium_featured' %>
</div>
